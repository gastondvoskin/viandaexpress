// actions

import axios from "axios"
import { getAllFoods/* , getFoodById */ } from "./foodSlice" 

/* export const getFoods = () => (dispatch)=> {
    axios("RUTA QUE PROVEERÁ EL BACKEND")
    .then(res => dispatch(getAllFoods(res.data)))
    .catch(error => console.log(error))
} */

export const getFoods = () => {

}